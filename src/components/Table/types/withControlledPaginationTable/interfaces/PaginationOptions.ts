/**
 * Represents the pagination options for the table.
 */
export interface PaginationOptions {
  /**
   * The index of the current page.
   */
  pageIndex: number;

  /**
   * The number of items per page.
   */
  pageSize: number;
}
