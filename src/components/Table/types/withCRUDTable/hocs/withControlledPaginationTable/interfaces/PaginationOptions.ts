/**
 * Represents the pagination options for a table.
 */
export interface PaginationOptions {
  /**
   * The index of the current page.
   */
  pageIndex: number;

  /**
   * The number of items to display per page.
   */
  pageSize: number;
}
